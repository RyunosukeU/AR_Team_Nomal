<div id="selection">
  <header>
      <h1>ランキング</h1>
      <div id="back_s" class="side_button">戻る</div>
  </header>
  <div id="stages">
      <% for(var stage of stages) { %>
          <p id="<%= stage.id %>" >  <%= stage.name %></p>
          <% } %>
  </div>
</div>